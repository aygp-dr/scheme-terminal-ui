#!/usr/bin/env guile
!#
;; Basic Terminal UI Example

(use-modules (ice-9 format))

(define (main)
  (format #t "Hello from Scheme Terminal UI Framework v0.1.0!~%")
  (format #t "This is a placeholder example.~%"))

(main)
